<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>tableau de notes</title>
</head>
<body>
    <h1>Le tableau de notes</h1>
     <p> Il y a: {{ notes | length }} notes </p>
    <table>
        {% for note in notes %}
            <p>{{ note }}</p>
        {% endfor %} 
    </table>
    <p>la note minimum est: {{ min(notes) }} </p>
    <p>la note maximum est: {{ max(notes) }} </p>
</body>
</html>
